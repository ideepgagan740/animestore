# Public values only. Never place secrets in NEXT_PUBLIC_* variables.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/api
NEXT_PUBLIC_APP_NAME=Anime Store

# Server-only values can exist without NEXT_PUBLIC_ when using Next server actions/routes.
# Keep real values in .env.local and never commit them.
